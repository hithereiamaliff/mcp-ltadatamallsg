# MCP Server - Docker Compose
# For self-hosting on VPS

services:
  mcp-ltadatamall:
    build: .
    container_name: mcp-ltadatamall
    restart: unless-stopped
    ports:
      - "8088:8080"
    environment:
      - PORT=8080
      - HOST=0.0.0.0
      - LTA_API_KEY=${LTA_API_KEY:-}
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - mcp-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  mcp-network:
    driver: bridge
